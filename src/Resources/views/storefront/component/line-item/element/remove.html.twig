{% sw_extends '@Storefront/storefront/component/line-item/element/remove.html.twig' %}

{% block component_line_item_remove %}
    {% if (lineItem.payload.isLogisticSurcharge is defined and lineItem.payload.isLogisticSurcharge) or (lineItem.payload.isCodFee is defined and lineItem.payload.isCodFee) %}
        {# Keine Anzeige des Entfernen-Buttons für die Zuschläge #}
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}
